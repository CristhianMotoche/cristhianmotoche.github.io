machine:
  ghc:
    version: 7.10.2
dependencies:
  cache_directories:
    - "~/.cabal"
    - "~/.stack"
    - ".stack-work"

  override:
    - mkdir -p .stack-work
    - bash ./install-stack.sh
    - stack setup
    - stack install --only-dependencies

  pre:
    - sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-4.6 20
    - sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-4.6 20
    - sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-4.9 10
    - sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-4.9 10

test:
  override:
    - stack build --fast -j2
  post:
    - git submodule init
    - git submodule update
    - cd _site/ && git checkout master
    - stack exec site build

deployment:
  production:
    branch: hakyll
    commands:
      - git config --global user.email cristhian.motoche@gmail.com
      - git config --global user.name CristhianMotoche
      - cd _site/ && git status
      - cd _site/ && git add --all
      - cd _site/ && git commit -m "Update (`date '+%F %T %Z'`) [ci skip]"
      - cd _site/ && git push origin master
      - git status
      - git add _site/
      - git commit -m "Update _site (`date '+%F %T %Z'`) [ci skip]"
      - git push origin hakyll
